# Default example

Minimal corp ACA runner deployment into existing ALZ vending subnets. The consumer owns AVNM, UDRs, hub firewall policy, and private DNS links.

```powershell
terraform init
terraform plan
```
